kind: pipeline
name: default
type: docker

clone:
  disable: true

steps:
- name: docker  
  image: plugins/docker
  settings:
    repo: expertControl/grafana
    tags: dev
  when:
    branch:
      - v7.1.x-expertControl